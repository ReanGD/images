version: '2'
services:
  jdk:
    build: .
    restart: unless-stopped
    image: reangd/alpine-jdk8:latest
    environment:
      - S6_LOGGING=0
      - CONSUL_SERVICENAME=java               # service name to register
    volumes:
      # - ./log:/var/log:rw
      - /etc/localtime:/etc/localtime:ro
